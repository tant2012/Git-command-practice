#Local repo
git commit / --amend\n
git branch /-f
git merge
git rebase /-i
git checkout /-b
git reset/git revert
git cherry-pick
git tags/git describe
git stash

#Remote repo
git clone
git fetch/git pull/ --rebase
git push
git checkout -b (remote tracking)
